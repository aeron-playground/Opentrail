# Copy to .env in this folder. Used by `bun run db:migrate`.
DATABASE_URL=postgres://app:app@localhost:5432/app

# Tests wipe this database, so its name must end in "_test". This value is also the default.
TEST_DATABASE_URL=postgres://app:app@localhost:5432/app_test
